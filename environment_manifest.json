{
  "system": {
    "user": "${USER}",
    "hostname": "${HOSTNAME}",
    "kernel": "auto",
    "architecture": "x86_64",
    "platform": "linux",
    "shell": "zsh",
    "session": "KDE Plasma 6, Wayland"
  },
  "resources": {
    "cpu": {
      "type": "auto-detect",
      "physical_cores": "auto",
      "logical_threads": "auto",
      "cache": "auto",
      "mode": "SMP",
      "kernel": "zen kernel (preempt dynamic)"
    },
    "ram": {
      "total": "auto",
      "available": "auto",
      "buff_cache": "auto",
      "swap": {
        "type": "file swap + zswap (kernel)",
        "swapfile_size": "auto",
        "swapfile_used": "auto",
        "zswap_enabled": true,
        "zswap_compressor": "zstd",
        "zswap_zpool": "z3fold"
      }
    },
    "storage": {
      "root": {
        "device": "auto-detect",
        "filesystem": "ext4",
        "size": "auto",
        "used": "auto",
        "available": "auto"
      },
      "home": {
        "device": "auto-detect",
        "filesystem": "btrfs",
        "size": "auto",
        "used": "auto",
        "available": "auto"
      }
    },
    "gpu": {
      "name": "auto-detect",
      "driver": "auto",
      "cuda_version": "auto",
      "vram_total": "auto",
      "vram_in_use": "auto",
      "vram_available": "auto"
    }
  },
  "constraints": [
    "Root (/) is ext4 — no CoW concern",
    "Home and /mnt/data are btrfs — swapfile on btrfs needs `compress=no` option",
    "zswap enabled (zstd compressor, z3fold zpool)",
    "Swapfile on btrfs: verify `compress=no` in fstab entry to avoid CoW overhead",
    "Manifest is ground truth — update when stale (> 7 days)"
  ],
  "last_updated": "${DATE}"
}
